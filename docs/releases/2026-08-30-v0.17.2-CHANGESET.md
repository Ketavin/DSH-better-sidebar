# CHANGESET: v0.17.2

Date: 2026-08-30 (Asia/Shanghai)

Status: corrected compatibility candidate; draft only; not promoted to the formal DSH profile.

## Frozen scope

- Base: upstream release `v0.17.1` at `3b1898f9cb74edf4ca542ff84430eaf346dd05f4`.
- Candidate branch: `Ketavin/0.17.1-arist-compat`.
- Candidate package/tag identity: stable patch `0.17.2` / `v0.17.2`; candidate status is expressed by the GitHub draft/prerelease state because the DSH community-market manifest rejects prerelease SemVer.
- No commit after upstream tag `v0.17.1` was imported from `origin/main`; that line was 32 commits ahead when frozen.
- No DSH core source, formal profile, formal process, API key, or Gateway code was changed.

## Compatibility and review-fix commits

| Commit | Purpose |
| --- | --- |
| `ce73f4f` | Make folder rows reliable expand/collapse controls while preserving drag/drop behavior. |
| `a87f658` | Refresh already-open editor tabs when a viewer registers after the file was opened. |
| `44f8204` | Initial Windows `AttachConsole` cleanup-race compatibility change. |
| `0018348` | Replace the workspace-only node-pty patch with a helper and runtime hook that ship inside the npm tarball. |
| `5222fea` | Drop stale cross-session pin activation, fail workspace pins closed, normalize workspace identity, and mount only the active virtual terminal. |
| `31f7717` | Refresh same-id HMR viewer replacements and map terminal URLs from xterm display cells so CJK/wide glyphs have correct coordinates. |
| `d1dc720` | Align package, plugin and service identity at `0.17.2`; make fork releases validation-only for npm publishing. |
| `43d89ec` | Avoid deferred named-signal rejection by using the default node-pty termination path on Windows. |

## Deep-review finding-to-fix map

1. **Packaged ConPTY fix was missing.** The old root `pnpm-workspace.yaml` patch did not travel with `pnpm pack`; a clean consumer silently received unpatched node-pty. The candidate now owns `scripts/conpty-console-list-agent.cjs` plus `src/windows-pty-cleanup.ts`, installs the hook on both user and agent PTYs, and carries no workspace patch.
2. **Pinned-terminal session isolation was fail-open.** Missing cwd values exposed workspace pins too broadly and stale local activation could override the next session's real active tab. Both paths now fail closed and Windows/UNC/POSIX workspace identity is normalized explicitly.
3. **Pinned terminals had unbounded client resources.** Every inactive virtual projection mounted xterm and a WebSocket. Only the active projection now mounts; the home-session PTY remains authoritative and parked.
4. **Viewer HMR missed same-id replacements.** Registry snapshots based only on sorted ids could not observe dispose/register of a new descriptor with the same id. Every registry mutation now advances a monotonic React revision.
5. **Terminal link ranges used UTF-16 offsets.** Links after or containing CJK, emoji, combining sequences, and wide cells could point at the wrong xterm columns. The provider now maps URL offsets through xterm cell widths.
6. **Release identity and publishing were unsafe.** Package, manifest, service, tag, artifact and workflow are aligned at `0.17.2`; releases in forks build/test/dry-run only and cannot attempt the upstream npm trusted-publisher identity.
7. **Windows named signals could throw asynchronously.** node-pty may defer `kill('SIGKILL')` until ConPTY becomes ready, outside the caller's `try/catch`. Windows now calls parameterless `kill()` directly; POSIX retains named signals.

## Changed surface relative to v0.17.1

- Release and packaging: `.github/workflows/release.yml`, `package.json`, `dsh.plugin.json`, package file list, release records.
- Server/runtime: `src/pty-manager.ts`, `src/agent-pty.ts`, `src/windows-pty-cleanup.ts`, `scripts/conpty-console-list-agent.cjs`.
- Client: `FileTree`, `EditorHost`, `Sidebar`, pinned projection rules, `TerminalView`, terminal-link mapping, and the related stylesheet.
- Regression coverage: folder toggle/drop, viewer HMR, pinned isolation/lifecycle, terminal wide-cell links, packaged ConPTY fallback, service/manifests and agent PTY behavior.

## Immutable package

- Asset: `dsh-better-sidebar-0.17.2.tgz`
- Package/manifest/service version: `0.17.2`
- Size: 3,436,410 bytes
- Entries: 281
- Unsafe archive paths: 0
- SHA-256: `CAEC710D6C93670DBDA0A5E45A1C5F9D499DC683E9578D1C7C3D66D27438D623`
- Distribution state: draft release only; not copied into the formal profile.
- Review: `https://github.com/Ketavin/DSH-better-sidebar/pull/2` (draft).

## Verification matrix

### Deterministic gates

- `pnpm install --frozen-lockfile`: pass.
- `pnpm build`: pass.
- `pnpm typecheck`: pass.
- Core review regression group: 168/168 pass.
- Agent PTY + packaged ConPTY group: 24/24 pass, repeated twice with clean worker exit.
- Manifest/service/package group: 96/96 pass.
- Browser-only consumer declaration check: pass with `skipLibCheck=true`; strict pass reports no Better Sidebar declaration error (only the pre-existing filtered upstream declaration noise).
- Release workflow YAML parse: pass.
- Clean install outside the repository: pass with peer auto-install disabled and lifecycle scripts ignored.
- Installed artifact probe: version `0.17.2`, signal fix present, helper present, no workspace patch/config, dead-PID fallback returned `[2147483647]`.

### Exact full-test failure-set comparison

Upstream `v0.17.1` frozen Windows baseline:

- Test files: 2 failed, 99 passed, 1 skipped (102 total).
- Tests: 4 failed, 1091 passed, 12 skipped (1107 total).

Corrected candidate final run:

- Test files: 2 failed, 101 passed, 1 skipped (104 total).
- Tests: 4 failed, 1107 passed, 12 skipped (1123 total).
- Unhandled errors: 0.

The failure set is exactly equal to the frozen baseline:

1. `tests/fs-operations.spec.ts > writeWorkspaceUpload > refuses upload directories and targets that resolve outside the workspace`
2. `tests/smoke.spec.ts > git destructive operations (scratch repository) > discard restores the worktree file from the index (staged changes kept)`
3. `tests/smoke.spec.ts > git destructive operations (scratch repository) > revert creates a revert commit`
4. `tests/smoke.spec.ts > git destructive operations (scratch repository) > cherry-pick applies a commit from another branch`

The first failure is the host's Windows symlink-creation permission; the remaining three are the frozen Git CRLF expectation set. No new failed test or hidden unhandled error remains.

## Canary status

- The earlier 3084 Canary proved the pre-review candidate's DSH/Arist integration, but it is not used as runtime evidence for this corrected artifact.
- A fresh random-port/scratch-home mount was attempted for `0.17.2`. It remained in external `npx @deepseek-ai/dsh` acquisition for about four minutes and never reached plugin installation or web startup, so it is classified as an external CLI acquisition timeout, not a product pass or failure.
- The interrupted Canary's three child processes were terminated by exact PID and its single scratch directory was removed; no Canary listener remained.
- Formal `127.0.0.1:3080` and Gateway `127.0.0.1:8002` were not restarted, rebound, reconfigured or used by the candidate.
- Promotion remains gated on one fresh real DSH mount when the CLI can be supplied locally or fetched successfully.

## Known non-blocking compatibility metadata

- `@huanlin/dsh-plugin-better-sidebar-plugin-office@0.1.1` declares peer `dsh-better-sidebar@^0.6.0`.
- `dsh-excel-panel@0.6.1` declares peer `dsh-better-sidebar@^0.12.0`.

Those ranges already excluded the formal `0.16.1` package. They are not changed in this candidate; the previous isolated integration run loaded both consumers.
