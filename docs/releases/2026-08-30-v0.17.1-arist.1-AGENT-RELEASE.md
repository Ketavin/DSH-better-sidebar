# AGENT-RELEASE: Better Sidebar v0.17.1-arist.1

## Decision

Produce a removable, reviewable compatibility candidate from the exact upstream `v0.17.1` release line. Do not promote it to formal DSH until the user separately approves promotion after reviewing the draft PR and Canary evidence.

## Release identity

- Source repository: `Ketavin/DSH-better-sidebar`
- Branch: `Ketavin/0.17.1-arist-compat`
- Base tag: `v0.17.1` (`3b1898f9cb74edf4ca542ff84430eaf346dd05f4`)
- Last code commit: `44f8204`
- Candidate tag: `v0.17.1-arist.1`
- Review: `https://github.com/Ketavin/DSH-better-sidebar/pull/2` (draft PR to the fork's `main` branch).
- Artifact SHA-256: `E5E3798FC508D9686B7F45C3AC3E8664A1984F4D278B627B000026AA65E402E8`

## Declared diff

Only three Arist compatibility deltas are allowed on top of upstream `v0.17.1`:

1. Folder row toggle reliability.
2. Open editor refresh after late file-viewer registration.
3. Windows node-pty `AttachConsole` cleanup-race handling.

## Effective diff

The effective diff equals the declared diff: 10 files, 356 insertions, 20 deletions. `origin/main` is not an ancestor of the candidate, so none of the 32 post-tag upstream commits are present.

## Locked variables

- Formal DSH address: `127.0.0.1:3080`.
- Formal Arist Gateway address: `127.0.0.1:8002`.
- Formal Better Sidebar artifact: `dsh-better-sidebar-0.16.1.tgz`.
- Formal artifact SHA-256: `87D98420F38CE0A562D2754075866A39363EDE7DB2221E578F8E07EF4E58737B`.
- Formal services were not restarted, rebound or reconfigured.
- No key was created, rotated, displayed or copied into source control.

## Promotion gate

Promotion is not part of this release candidate. A later promotion turn must independently verify:

1. The draft PR contains only the declared upgrade and compatibility commits.
2. The draft prerelease asset hash equals the value above.
3. Formal 3080 and Gateway 8002 are healthy immediately before change.
4. The exact failure set is unchanged, not merely the failure count.
5. A rollback copy of the formal `0.16.1` artifact and profile lockfiles exists before replacement.
6. The user gives explicit approval for formal promotion.

## Rollback

If a later formal promotion fails:

1. Stop only the newly promoted 3080 process.
2. Restore the formal profile dependency to `file:packages\\release\\dsh-better-sidebar-0.16.1.tgz`.
3. Restore the pre-promotion `package.json`, `pnpm-lock.yaml` and package artifact from the timestamped rollback directory.
4. Run the profile's frozen install command.
5. Start the formal web process on 3080 with the existing wrapper and environment.
6. Verify the formal artifact SHA-256 above, `GET /`, Finance registration and terminal open/close.
7. Do not touch Gateway 8002 unless independent evidence identifies it as the fault domain.

## Cleanup contract

After the draft PR and draft prerelease retain the source and immutable artifact, stop port 3084, close the agent-created browser tab, remove the isolated DSH home, logs, fixtures, build output, dependency install and the single rolling worktree, prune worktree metadata, and verify that only the canonical main worktree remains. Formal 3080/8002 must remain on their original PIDs throughout.
