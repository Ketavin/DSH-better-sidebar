# CHANGESET: v0.17.1-arist.1

Date: 2026-08-30 (Asia/Shanghai)

Status: compatibility candidate; not promoted to the formal DSH profile.

## Frozen scope

- Base: upstream signed release line `v0.17.1` at `3b1898f9cb74edf4ca542ff84430eaf346dd05f4`.
- Candidate branch: `Ketavin/0.17.1-arist-compat`.
- No commit after upstream tag `v0.17.1` was imported from `origin/main`; that line was 32 commits ahead when frozen.
- No DSH core source, formal profile, formal process, API key, or Gateway code was changed.

## Compatibility commits

| Candidate commit | Upstream/fork source | Purpose |
| --- | --- | --- |
| `ce73f4f` | `4867943` | Make folder rows reliable expand/collapse controls while preserving drag/drop behavior. |
| `a87f658` | `2c3ef97` | Refresh already-open editor tabs when a viewer registers after the file was opened. |
| `44f8204` | `2904149` | Patch `node-pty@1.1.0` so Windows `AttachConsole` cleanup races do not produce repeated failures. |

## Changed files relative to v0.17.1

- `patches/node-pty@1.1.0.patch`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml`
- `src/client/EditorHost.tsx`
- `src/client/FileTree.tsx`
- `src/client/sidebar.module.css`
- `tests/editor-host.spec.tsx`
- `tests/file-tree-drop.spec.tsx`
- `tests/file-tree-toggle.spec.tsx`
- `tests/node-pty-attachconsole.spec.ts`

Diff size: 356 insertions and 20 deletions across 10 files.

## Immutable package

- Asset: `dsh-better-sidebar-0.17.1-arist.1.tgz`
- Package metadata version: `0.17.1`
- Size: 3,429,311 bytes
- Entries: 278
- Unsafe archive paths: 0
- SHA-256: `E5E3798FC508D9686B7F45C3AC3E8664A1984F4D278B627B000026AA65E402E8`
- Distribution state: draft prerelease only; not copied into the formal profile.

## Verification matrix

### Build and focused tests

- `pnpm install --frozen-lockfile`: pass.
- `pnpm typecheck`: pass.
- `pnpm build`: pass.
- Focused compatibility suite: 24/24 pass.
- Consumer TypeScript fixture with `skipLibCheck=true`: pass.
- With `skipLibCheck=false`, only the pre-existing `@deepseek-ai/cosmokit` typed-array generic errors remain; there is no Better Sidebar consumer error.

### Exact full-test failure-set comparison

Upstream `v0.17.1` baseline:

- Test files: 2 failed, 99 passed, 1 skipped (102 total).
- Tests: 4 failed, 1091 passed, 12 skipped (1107 total).

Candidate final clean run:

- Test files: 2 failed, 101 passed, 1 skipped (104 total).
- Tests: 4 failed, 1098 passed, 12 skipped (1114 total).

The candidate failure set is exactly equal to the frozen upstream baseline:

1. `tests/fs-operations.spec.ts > writeWorkspaceUpload > refuses upload directories and targets that resolve outside the workspace`
2. `tests/smoke.spec.ts > git destructive operations (scratch repository) > discard restores the worktree file from the index (staged changes kept)`
3. `tests/smoke.spec.ts > git destructive operations (scratch repository) > revert creates a revert commit`
4. `tests/smoke.spec.ts > git destructive operations (scratch repository) > cherry-pick applies a commit from another branch`

The first failure is a Windows symlink-creation permission failure; the remaining three are Windows CRLF expectations. No new failure was introduced. A transient Windows rename `EPERM` in an earlier concurrent-upload run passed on direct rerun and on 20 repeated targeted runs, so it is not in the frozen final failure set.

The final clean run contained 0 `AttachConsole failed` markers and 0 unhandled-error markers.

## Isolated Canary evidence

- Runtime: one isolated `DSH_HOME`, one web Canary on `127.0.0.1:3084`, one session workspace, no new credentials.
- Installed package: real directory, version `0.17.1`; patched `node-pty@1.1.0` marker verified.
- Folder toggle: nested row expanded to reveal one child and collapsed back to zero children.
- Late viewer registration: DOCX, XLSX and PPTX files opened from the file tree after consumer registration.
- Consumers loaded without page errors: Arist Finance, Document Workbench, Excel Panel, Office PPTX viewer, SSH Tunnel and Arist Development Dashboard.
- Finance returned `status=success` for NVDA with 16 datasets and rendered market, financials, latest news, annual reports, research views, valuation, citations and truthful fallbacks.
- Better Sidebar tabs survived a full page reload with the same open-tab set.
- Windows terminal: 21 additional create/close cycles, 0 remaining terminal tabs, 0 residual terminal process, 0 `AttachConsole` marker.
- Visual checks: normal desktop viewport and 464x900 responsive viewport both remained readable; the temporary viewport override was reset.
- Browser diagnostics: 0 errors. Warnings were confined to hidden Finance charts temporarily measuring 0x0 and the intentionally tiny PPTX fixture reporting empty XML; neither was emitted by Better Sidebar.

## Known non-blocking compatibility metadata

- `@huanlin/dsh-plugin-better-sidebar-plugin-office@0.1.1` declares peer `dsh-better-sidebar@^0.6.0`.
- `dsh-excel-panel@0.6.1` declares peer `dsh-better-sidebar@^0.12.0`.

Those peer ranges already excluded the formal `0.16.1` package. Both consumers loaded in the Canary, so this candidate does not broaden scope to republish those packages solely to update peer metadata.
